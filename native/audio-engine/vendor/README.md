# miniaudio

Colocar aquí `miniaudio.h` (https://github.com/mackron/miniaudio) y compilar con
`JASWAVE_USE_MINIAUDIO=1` para salida hardware.

El MVP usa el device stub en `src/engine.cpp` (hilo de audio sin I/O).
